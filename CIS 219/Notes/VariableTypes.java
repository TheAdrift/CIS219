public class VariableTypes
{
	public static void main(String [] args)
	{
		int age = 50; //ALL int USE FOUR BYTES. A short only uses (missed it. look up later)
		byte classSize = 15; //byte reserves one byte?
		long number = 7777777777l; //this is a long. 64 bit signed. (Chart in section 2.8)
		float taxRate = 0.08f;
		double income = 56000.99;
		char myInitial = 'C';
		String myName = "Cam";
		double radius = 5.5;
		double answer = Math.pow(radius, 2);
	}
}